export class Player {
    name  : string;
    id    : string;
    score : number;

    constructor(name: string, id: string, score: number)  {
        this.name  = name;
        this.id    = id;
        this.score = score;
    }
}